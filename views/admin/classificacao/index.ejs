<!DOCTYPE html>
<html lang="pt-br">
<%- include("../../includes/head") %>
  <body>
    <div class="corpoPrincipal">
<%- include("../../includes/header") %>
    <main class="conteudoPrincipal">

    <h1><%= title %></h1>

    <p><a href="/admin/rating/new"><button type="button">Adicionar</button></a></p>

    <table class="tabelaDados">
      <thead>
        <tr>
          <th scope="col">ID</th>
          <th scope="col">Classificação</th>
          <th scope="col">Ações</th>
        </tr>
      </thead>
      <tbody>
        <% classificacoes.forEach(classificacao => { %>
        <tr>
          <th scope="row"> <%= classificacao.id %></th>
          <th> <%= classificacao.nome %></th>
          <th>
            <a href="/admin/rating/edit/<%= classificacao.id %>"><button type="button">Editar</button></a>
            <form action="/admin/rating/delete/<%= classificacao.id %>?_method=DELETE" method="POST">
              <button type="submit">Excluir</button>
            </form>
          </th>
        </tr>
        <% }); %>
      </tbody>
    </table>

    </main>
<%- include("../../includes/footer") %>
<%- include("../../includes/scripts") %>
    </div>
  </body>
</html>
